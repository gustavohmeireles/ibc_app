<%= form_with(model: animal) do |form| %>
  <% if animal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(animal.errors.count, "error") %> "Animal" não foi salvo:</h2>

      <ul>
        <% animal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :criacao_do_animal, style: "display: block" %>
    <%= form.text_field :criacao_do_animal %>
  </div>

  <div>
    <%= form.label :raca, style: "display: block" %>
    <%= form.text_field :raca %>
  </div>

  <div>
    <%= form.label :idade_do_animal, style: "display: block" %>
    <%= form.number_field :idade_do_animal %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
